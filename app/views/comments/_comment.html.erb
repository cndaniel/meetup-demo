<li>
  <span><%= comment.user.email %></span>
  <span class="date"><%= "at #{comment.id.ordinalize} floor" %></span>
  <span><%= time_ago_in_words(comment.created_at) %> ago</span>
  <p>
    <%= comment.content %>
  </p>
</li>
